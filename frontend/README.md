# Frontend (Vite + React)

## Setup

```bash
cd frontend
npm install
cp .env.example .env
npm run dev
```

## Env

See `.env.example`. Point `VITE_API_URL` at the omni process (platform `/api/*`).

## Icons

Copied from `docs/cow.png` and `docs/agents/*` into `public/`.
